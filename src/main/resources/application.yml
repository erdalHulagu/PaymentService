server: 
 port: 5005
 error: 
  include-binding-errors: always
  include-message: always
  include-exception: true
  
spring:
 datasource:
  hikari:
   autoCommit : false
  url: jdbc:postgresql://localhost:5433/paymentService?createDatabaseIfNotExist=true
  username: postgres
  password: aylaadem233
  driver-class-name: org.postgresql.Driver
  
 jpa:
  hibernate: 
   ddl-auto: update
  show-sql: true
  database-platform: org.hibernate.dialect.PostgreSQLDialect
  properties:
   '[hibernate.format_sql]': true
   
 goCardless :
  api :
   key : 'your goCardless api key'
   secret : 'your goCardless api secret'
   
 stripe :
  api :
   key : 'your stripe api key'
   secret : 'your stripe api secret'

info:
 application: 
  name: 'category service' 
  description: 'This is a Payment microservice for saloon booking system'
  version: '@project.version@' 
   

